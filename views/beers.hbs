<div class='beers-container'>
  {{#each beersFromApi}}
    <div class='beer-card'>
      <img src='{{this.image_url}}' alt='{{this.name}}' />
      <h3>{{this.name}}</h3>
      <p>{{this.tagline}}</p>
      <p>{{this.description}}</p>
    </div>
  {{/each}}
</div>